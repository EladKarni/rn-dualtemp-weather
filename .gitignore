node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
api-gcs.*

# Environment variables — ignore all env files, keep the committed example
.env
.env.*
!.env.example

# Native Code (Expo prebuild output at the repo root only — nested ios/ and
# android/ dirs such as scripts/widget-screenshots/ios/ are tracked)
/android/
/ios/

# macOS
.DS_Store

# Play Store — never commit the real service-account key (eas submit writes it here)
serviceKey*.json
!serviceKey.example.json